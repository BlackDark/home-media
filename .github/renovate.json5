{
  extends: ["github>linkvt/renovate-base", "schedule:weekly"],
  kubernetes: {
    fileMatch: ["kubernetes/.+\\.ya?ml$"],
  },
  packageRules: [
    {
      versioning: "regex:^(?<prerelease>develop-)?v?(?<major>\\d+)\\.?(?<minor>\\d+)?\\.?(?<patch>\\d+)?(\\.|-)?(?<build>\\w?\\d+)?-ls\\d+$",
      matchPackagePatterns: ["(^|\\/)linuxserver\\/.*$"],
    },
    {
      customChangelogUrl: "https://github.com/goauthentik/authentik",
      matchPackageNames: ["authentik"],
    },
  ],
  regexManagers: [
    {
      datasourceTemplate: "helm",
      fileMatch: ["helm-chart\\.ya?ml$"],
      matchStringsStrategy: "combination",
      matchStrings: [
        "repo:\\s+(?<registryUrl>.*)",
        "chartName:\\s+(?<depName>.*)",
        "chartVersion:\\s+(?<currentValue>.*)",
      ],
    },
  ],
}
