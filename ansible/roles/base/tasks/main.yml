- name: install aptitude
  apt:
    name: aptitude
    state: latest
    update_cache: true

- name: install base packages
  apt:
    pkg:
      - apt-transport-https
      - ca-certificates
      - curl
      - software-properties-common
    state: latest
    update_cache: true
