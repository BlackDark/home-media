apiVersion: apps/v1
kind: Deployment
metadata:
  name: sabnzbd
spec:
  selector:
    matchLabels:
      app: sabnzbd
  template:
    metadata:
      labels:
        app: sabnzbd
    spec:
      volumes:
        - name: app-data
          persistentVolumeClaim:
            claimName: sabnzbd-local
        - name: media
          persistentVolumeClaim:
            claimName: media-downloads
      containers:
        - name: sabnzbd
          image: linuxserver/sabnzbd:4.2.1-ls144
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: common-deployment-environment
          volumeMounts:
            - mountPath: /config
              name: app-data
            - mountPath: /downloads
              name: media
              subPath: usenet/
            - mountPath: /incomplete-downloads
              name: media
              subPath: usenet/_incomplete

---
apiVersion: v1
kind: Service
metadata:
  name: sabnzbd
spec:
  ports:
    - port: 80
      name: http
      targetPort: http
  selector:
    app: sabnzbd

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sabnzbd-basic
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.middlewares: media-basic-auth@kubernetescrd
spec:
  rules:
    - host: sabnzbd-basic.linkvt.de
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: sabnzbd
                port:
                  name: http

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sabnzbd
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.middlewares: authentik-auth@kubernetescrd
spec:
  rules:
    - host: sabnzbd.linkvt.de
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: sabnzbd
                port:
                  name: http

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sabnzbd-local
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
