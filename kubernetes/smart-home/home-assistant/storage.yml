apiVersion: v1
kind: PersistentVolume
metadata:
  name: home-assistant-app-data
spec:
  storageClassName: ""
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/apps/home-assistant"
    type: DirectoryOrCreate

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-assistant-app-data
spec:
  storageClassName: "" # Empty string must be explicitly set otherwise default StorageClass will be set
  volumeName: home-assistant-app-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
