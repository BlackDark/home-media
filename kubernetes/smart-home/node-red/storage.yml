apiVersion: v1
kind: PersistentVolume
metadata:
  name: node-red-app-data
spec:
  storageClassName: ""
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/apps/node-red"
    type: DirectoryOrCreate

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: node-red-app-data
spec:
  storageClassName: "" # Empty string must be explicitly set otherwise default StorageClass will be set
  volumeName: node-red-app-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
